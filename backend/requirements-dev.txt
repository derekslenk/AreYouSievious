# Backend development dependencies.
#
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Test runner + asyncio support for ASGI integration tests
pytest>=8
pytest-asyncio>=0.23
# ASGI HTTP client driven by tests/test_import_event_loop.py
httpx>=0.27

# Lint + format + type-check (configured in pyproject.toml)
ruff>=0.7
basedpyright>=1.18

# Reads .github/workflows/*.yml in tests/test_ci_gates_every_pr.py, which
# locks the triggers that decide whether CI runs at all. Declared rather
# than assumed: pre-commit pulls PyYAML in transitively, so the test passed
# locally and would have failed in CI, where only these files are installed.
PyYAML>=6

# Pre-commit hooks
pre-commit>=3.7
