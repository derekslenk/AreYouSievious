"""
Health-check router skeleton (areyousievious-u40 placeholder).

The /healthz endpoint and JSON audit logging are owned by bd issue
areyousievious-ilm. This file exists so the router package shape is
complete after u40 — ilm will fill it in.
"""

from fastapi import APIRouter

router = APIRouter()
